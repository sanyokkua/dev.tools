import type { SkillDef } from '../../model/types.ts';

export const skill: SkillDef = {
    id: 'SKILL-test-runner',
    slug: 'test-runner',
    domainCode: 'A',
    title: 'Test Runner',
    version: '2.0.0',
    description:
        'Generate tests for target code in a repository using the project\'s existing test framework and conventions, then run them and iterate until passing or until a real defect is found. Use when the user asks to "write and run tests", "add tests for X and make sure they pass", or "increase coverage for this module". Writes test files and executes the test command; does not change production code except to unblock an obvious test-harness issue (which it flags). Boundaries: defers framework and layout discovery to a project-navigation step and review of the change under test to a code-review step; never claims a pass without an actual run; never weakens a test to hide a real product bug.',
    tags: [
        'testing',
        'unit-tests',
        'integration-tests',
        'run-tests',
        'coverage',
        'framework-detection',
        'mocking',
        'repository',
    ],
    allowedTools: ['Read', 'Grep', 'Glob', 'Write', 'Edit', 'Bash'],
    relatedSkillIds: ['SKILL-project-navigator', 'SKILL-code-review'],
    files: [
        {
            path: 'SKILL.md',
            role: 'skill',
            content:
                '---\nname: test-runner\ntitle: Test Runner\nversion: 2.0.0\ndescription: >\n  Generate tests for target code in a repository using the project\'s existing test framework and\n  conventions, then run them and iterate until passing or until a real defect is found. Use when the\n  user asks to "write and run tests", "add tests for X and make sure they pass", or "increase coverage\n  for this module". Writes test files and executes the test command; does not change production code\n  except to unblock an obvious test-harness issue (which it flags). Boundaries: defers framework and\n  layout discovery to a project-navigation step and review of the change under test to a code-review\n  step; never claims a pass without an actual run; never weakens a test to hide a real product bug.\ntags: [testing, unit-tests, integration-tests, run-tests, coverage, framework-detection, mocking, repository]\nallowed-tools: Read, Grep, Glob, Write, Edit, Bash\nreferences:\n  - references/framework-detection.md\n  - references/mocking-boundaries.md\n  - references/coverage-tactics.md\nscripts:\n  - scripts/detect-and-run-tests.sh\nassets:\n  - assets/test-report-template.md\nrelated-skills:\n  - project-navigator: detect the test framework and layout first if unknown\n  - code-review: review the change the tests cover\ninstall:\n  defaultLocation: .claude/skills/test-runner/\n  supportsProject: true\n  supportsGlobal: true\n---\n\n# Test Runner\n\nYou are a test engineer who writes tests matching the repository\'s conventions and proves they pass by\nrunning them.\n\n## When to use\n\n"Write and run tests for X", "add tests and make them pass", "raise coverage for this module".\n\n## Workflow\n\n1. **Detect the test setup.** Run `scripts/detect-and-run-tests.sh --detect` to identify the\n   framework and the run command from manifests and CI configuration. Confirm with\n   `references/framework-detection.md` and by reading existing tests for directory and naming\n   conventions and fixtures. Match them exactly.\n2. **Analyze the target and enumerate cases.** Edge, boundary, and error cases first, plus the happy\n   path. List the cases before writing any code.\n3. **Write tests.** Mock only true external boundaries (network, database, clock, filesystem,\n   third-party services) per `references/mocking-boundaries.md`; never mock the logic under test. Put\n   files where the repository expects them.\n4. **Run them.** Execute the project\'s test command via `scripts/detect-and-run-tests.sh --run`; read\n   failures and iterate. For improving coverage, use `references/coverage-tactics.md` (target\n   meaningful assertions and uncovered branches, not a line-percentage number).\n5. **Handle failures honestly.** If a test reveals a real product bug, STOP weakening it — report the\n   bug. Only edit production code to fix an obvious test-harness blocker, and flag that you did so.\n6. **Report real results** using `assets/test-report-template.md`. Never claim passing without a run.\n\n## Mandatory validation (before finishing)\n\n- [ ] Tests follow the repository\'s framework and conventions.\n- [ ] Only external boundaries are mocked.\n- [ ] Tests were actually executed; results reported truthfully (pass/fail counts come from the run).\n- [ ] Any product bug discovered is reported, not hidden by a weakened assertion.\n- [ ] Production code is unchanged, except for a flagged harness fix.\n\n## Output format and location\n\nTest files are written into the repository\'s test locations. Then provide a verification summary using\nthe template: test files added, scenarios covered, framework and command used, run results (real\npass/fail counts), product bugs found, and coverage gaps. End with `TESTS_COMPLETE`.\n\n## Gotchas\n\n- A flaky environment (missing services) is not failing code — distinguish and report it.\n- Coverage is a flashlight, not a goal — meaningful assertions over a line percentage.\n- Do not introduce a new test dependency unless the repository already uses it (or the user approves).\n- Long-running or end-to-end suites: scope to the target unless the full run is asked for.\n- `Bash` runs in the workspace sandbox; ensure the repository path and toolchain are available before\n  claiming results.\n',
            bytes: 4179,
        },
        {
            path: 'references/framework-detection.md',
            role: 'reference',
            content:
                '# Test Framework Detection and Run Commands\n\nDetect the test framework from the repository\'s manifests and configuration files, then use the\n**project\'s own** run command (scripts, task runner, or build tool) rather than inventing one. Order of\nevidence, strongest first:\n\n1. **An explicit test script / task** the project already defines (e.g. `npm test`, a `Makefile`\n   `test` target, a `justfile`/`taskfile` recipe, a CI job\'s test step). Prefer this — it encodes the\n   real, intended invocation.\n2. **Continuous Integration config** (`.github/workflows/*.yml`, `.gitlab-ci.yml`,\n   `.circleci/config.yml`, `azure-pipelines.yml`): the exact command CI runs is the ground truth.\n3. **The framework\'s config file** (below).\n4. **The dependency manifest** listing a test framework.\n\nIf several frameworks are present (common in polyglot or migrating repos), detect all and ask which the\ntarget belongs to, or infer from the target file\'s language and the nearest existing tests.\n\n## Per-ecosystem detection table\n\n| Ecosystem | Manifest / signal files | Framework signals | Typical run command | Coverage flag |\n|---|---|---|---|---|\n| **Python** | `pyproject.toml`, `setup.cfg`, `tox.ini`, `pytest.ini`, `requirements*.txt` | `pytest`, `unittest`, `nose2` | `pytest` / `python -m pytest` / `python -m unittest` | `pytest --cov=<pkg> --cov-report=term-missing` (needs `pytest-cov`) |\n| **JavaScript / TypeScript** | `package.json` (`scripts.test`, `devDependencies`), `jest.config.*`, `vitest.config.*`, `.mocharc*`, `playwright.config.*` | `jest`, `vitest`, `mocha`, `ava`, `playwright`, `cypress` | `npm test` / `npx jest` / `npx vitest run` / `npx mocha` | `jest --coverage`, `vitest run --coverage` |\n| **Java** | `pom.xml` (Maven), `build.gradle`/`build.gradle.kts` (Gradle) | JUnit 4/5, TestNG; runner = Surefire/Failsafe | `mvn test` / `mvn -q test` ; `./gradlew test` | `mvn jacoco:report` ; `./gradlew jacocoTestReport` |\n| **Go** | `go.mod` | built-in `testing` (`*_test.go`) | `go test ./...` | `go test -coverprofile=cover.out ./... && go tool cover -func=cover.out` |\n| **Rust** | `Cargo.toml` | built-in `#[test]`; `cargo nextest` if configured | `cargo test` / `cargo nextest run` | `cargo tarpaulin` or `cargo llvm-cov` (if installed) |\n| **C# / .NET** | `*.csproj`, `*.sln`, `Directory.Build.props` | xUnit, NUnit, MSTest | `dotnet test` | `dotnet test --collect:"XPlat Code Coverage"` |\n| **Ruby** | `Gemfile`, `.rspec`, `Rakefile` | RSpec, Minitest | `bundle exec rspec` / `rake test` | `simplecov` (auto via setup) |\n| **PHP** | `composer.json`, `phpunit.xml` | PHPUnit, Pest | `vendor/bin/phpunit` / `vendor/bin/pest` | `phpunit --coverage-text` (needs Xdebug/PCOV) |\n\n## Disambiguating Maven vs. Gradle\n\n- `pom.xml` present → Maven (`mvn test`).\n- `build.gradle` / `build.gradle.kts` / `gradlew` present → Gradle (`./gradlew test`).\n- Both present (rare, migration) → prefer the one CI uses; the wrapper (`gradlew`/`mvnw`) being checked\n  in is a strong hint of the intended tool.\n- **Always prefer the checked-in wrapper** (`./gradlew`, `./mvnw`, `npx`) over a globally installed\n  binary — it pins the version the project expects.\n\n## Narrowing the run to the target\n\nRunning the whole suite for one new test is slow. Once detected, scope the run:\n\n| Framework | Run only the target file/test |\n|---|---|\n| pytest | `pytest path/to/test_x.py::TestClass::test_case` |\n| Jest/Vitest | `npx jest path/to/x.test.ts -t "case name"` |\n| Maven | `mvn test -Dtest=ClassName#methodName` |\n| Gradle | `./gradlew test --tests "pkg.ClassName.method"` |\n| go test | `go test ./pkg -run TestName` |\n| Cargo | `cargo test test_name` |\n| dotnet | `dotnet test --filter "FullyQualifiedName~Namespace.Class.Method"` |\n\nRun the scoped test while iterating; run the broader suite once before reporting to catch regressions.\n\n## Conventions to copy from existing tests\n\nRead one or two existing tests in the same area and match: directory (`tests/`, `__tests__/`,\n`src/test/java`, `*_test.go` colocated), file naming, import style, fixture/setup patterns, assertion\nlibrary, and how mocks/doubles are created. Consistency matters more than personal preference.\n\n## Graceful degradation\n\nIf the toolchain is not installed (the runner binary is missing), do **not** fabricate results: report\nthe detected framework and the exact command that *would* run, and state that execution was not\npossible in the current environment. The `detect-and-run-tests.sh` script reports this honestly.\n',
            bytes: 4498,
        },
        {
            path: 'references/mocking-boundaries.md',
            role: 'reference',
            content:
                '# Mocking: What to Mock and What to Never Mock\n\nThe single rule that keeps tests valuable: **mock at the system\'s true boundaries; never mock the logic\nunder test.** A test that mocks the thing it is supposed to verify proves only that the mock was called\n— it gives false confidence and breaks on every refactor.\n\n## The decision rule\n\nAsk of each dependency: *"Is this code I own and want to verify, or is it the edge of my system?"*\n\n- **Edge of the system → replace with a test double.** Anything non-deterministic, slow, external, or\n  outside the unit\'s responsibility.\n- **My own logic → run it for real.** The collaborators that make up the behavior you are testing.\n\n## Mock these (true external boundaries)\n\n- **Network / HTTP / RPC calls** to other services or third-party APIs.\n- **Databases and caches** — unless you are explicitly writing an integration test against a real\n  (containerized) instance.\n- **The clock and timers** — wrap `now()`/`sleep` so tests are deterministic and fast. Freeze or inject\n  time rather than asserting on wall-clock.\n- **Randomness / UUID generation** — inject a seeded or fixed source.\n- **The filesystem** — use a temp dir or an in-memory FS for unit tests.\n- **Message queues / event buses / pub-sub.**\n- **Environment and external configuration** — inject rather than reading global state.\n- **Email/SMS/payment/notification gateways** and any side-effecting third party.\n\n## Never mock these\n\n- **The function or class under test** — obviously, but it sneaks in via over-mocking its helpers.\n- **Pure logic, value objects, calculations, mappers, validators** — these are fast and deterministic;\n  running them for real is the whole point.\n- **Language/standard-library data structures** (lists, maps, strings).\n- **The framework\'s own machinery** you do not own — test through it, not around it.\n\n## Test-double taxonomy (use the right kind)\n\n| Double | Purpose | When |\n|---|---|---|\n| **Dummy** | Fills a parameter, never used | Satisfy a signature |\n| **Stub** | Returns canned data | Provide inputs from a boundary (e.g. "the API returns this JSON") |\n| **Fake** | Working lightweight implementation | In-memory DB/repo for fast, realistic behavior |\n| **Spy** | Stub that records calls | Verify an outbound side effect happened |\n| **Mock** | Pre-programmed with expectations, verifies interaction | Assert a specific collaboration contract |\n\nPrefer **stubs and fakes** for inputs; reserve **mocks/spies** for verifying genuine outbound effects\n(an email was sent, the queue received a message). Over-using interaction mocks couples tests to\nimplementation detail.\n\n## Classical vs. mockist (state vs. interaction)\n\n- **State-based (classical):** call the unit, assert on the returned value or resulting state. Robust to\n  refactoring. Default to this.\n- **Interaction-based (mockist):** assert that specific collaborators were called in a specific way. Use\n  only when the *interaction itself* is the behavior (e.g. "publishes exactly one event"). It is more\n  brittle — every internal change can break it.\n\nRule of thumb: **assert on outcomes, not on the number of internal calls,** unless the call *is* the\noutcome.\n\n## Boundaries by test level\n\n- **Unit tests:** mock all external boundaries; run all owned logic. Fast, many.\n- **Integration tests:** use real adapters at one or more boundaries (a real DB via a container, a real\n  HTTP server) to verify wiring and queries; mock only the boundaries you are not exercising.\n- **End-to-end tests:** mock as little as possible; exercise the real system. Few, slow, high value for\n  critical paths only (the test pyramid: many unit, fewer integration, fewest E2E).\n\n## Smells that mean you are mocking wrong\n\n- The test breaks when you refactor without changing behavior → too many interaction mocks.\n- You mock the class under test or its private helpers → you are testing the mock, not the code.\n- Setup is longer than the assertion and full of `when(...).thenReturn(...)` chains → the unit has too\n  many dependencies (a design smell the test is exposing — consider it real feedback).\n- A mock returns something so unrealistic the test passes but production fails → make the double behave\n  like the real boundary (use a fake) or write an integration test.\n\n## Ecosystem tools (use what the repo already uses)\n\n| Ecosystem | Common tools |\n|---|---|\n| Python | `unittest.mock` / `pytest` fixtures + `monkeypatch`, `freezegun` (clock), `responses`/`httpx mock` |\n| JS/TS | `jest`/`vitest` mocks, `msw` (network), `sinon`, `@sinonjs/fake-timers` |\n| Java | Mockito, WireMock (HTTP), Testcontainers (real DB), `Clock` injection |\n| Go | interfaces + hand-written fakes, `httptest`, `gomock`/`testify/mock` |\n| .NET | Moq/NSubstitute, WireMock.Net, Testcontainers |\n\nDo not introduce a new mocking library if the project already standardizes on one.\n',
            bytes: 4892,
        },
        {
            path: 'references/coverage-tactics.md',
            role: 'reference',
            content:
                '# Coverage Tactics: Meaningful Coverage, Not a Number\n\nCoverage is a **flashlight, not a goal.** It tells you which code your tests never executed — useful for\nfinding blind spots — but a high percentage with weak assertions proves nothing. Raise coverage by\ncovering *behaviors and branches that matter*, not by chasing a line-percentage target.\n\n## Understand the coverage metrics\n\n- **Line coverage:** which lines ran. Easiest to game (executing a line ≠ asserting its behavior).\n- **Branch / decision coverage:** which *outcomes* of each `if`/`switch`/ternary ran. Far more\n  meaningful — aim here.\n- **Condition coverage:** each boolean sub-expression evaluated both true and false (important for\n  compound conditions like `a && b`).\n- **Path coverage:** combinations of branches. Exhaustive coverage is usually impractical; target the\n  realistic paths.\n\nPrefer branch coverage as the working metric. A line-coverage jump with no new assertions is a warning\nsign, not progress.\n\n## How to find what to test next\n\n1. Run coverage with a **per-line/per-branch** report (`--cov-report=term-missing`, `jest --coverage`,\n   JaCoCo HTML, `go tool cover -func`/`-html`). Read the **uncovered branches**, not the headline\n   number.\n2. Triage the gaps by risk: error handling, edge/boundary inputs, security-relevant branches, and\n   money/correctness logic first. Trivial getters and generated code last (or exclude them).\n3. For each meaningful gap, write a test that **forces that branch** with a realistic input and asserts\n   the *outcome* (return value, state change, raised error, emitted effect) — not merely that the line\n   ran.\n\n## Case-enumeration checklist (write these before coding)\n\nFor the target function/module, enumerate:\n\n- **Happy path(s)** — typical valid input(s).\n- **Boundaries** — empty, zero, one, max, off-by-one, just-inside/just-outside limits.\n- **Invalid input** — null/None, wrong type, malformed, out of range → assert the error/handling.\n- **Error paths** — each `catch`/`except`/`if err != nil`; simulate the failure at the boundary and\n  assert recovery/propagation.\n- **State transitions** — each meaningful state and the transitions between them.\n- **Concurrency / ordering** — if relevant, idempotency and race-sensitive paths.\n- **Equivalence partitions** — one representative per class of input rather than dozens of near-duplicates.\n\nThis list, not the coverage tool, drives the tests; coverage then confirms you missed nothing important.\n\n## Assertion quality\n\n- Assert the **specific** expected value/state, not just "not null" or "no exception".\n- One logical behavior per test; a clear name describing the scenario and expectation.\n- For errors, assert the **type and message/code**, not merely that *something* threw.\n- Avoid asserting on incidental implementation detail (internal call counts) unless the interaction is\n  the behavior — see `mocking-boundaries.md`.\n\n## What NOT to do (anti-patterns)\n\n- Writing tests that call code but assert nothing, just to color lines green.\n- Lowering a meaningful assertion so a failing test passes (hides a real bug — report the bug instead).\n- Setting a coverage gate as the objective and writing filler tests to clear it.\n- Testing generated, vendored, or trivial pass-through code to inflate the number — exclude it from\n  coverage config instead.\n- Deleting or skipping a failing test to "improve" results.\n\n## Setting a sensible target\n\nA team-chosen threshold (often 70–85% line, with higher branch coverage on critical modules) is a\nguardrail to prevent regressions, not a destination. When asked to "increase coverage", report the\n**new behaviors now tested and the remaining high-risk gaps**, alongside the number — the gaps are the\ndeliverable, not the percentage.\n',
            bytes: 3801,
        },
        {
            path: 'scripts/detect-and-run-tests.sh',
            role: 'script',
            content:
                '#!/bin/sh\n# detect-and-run-tests.sh — Detect the test framework from manifests and run the suite.\n#\n# Inspects a repository\'s manifest and config files to identify the test framework and the correct run\n# command, then (optionally) runs it and emits a STRUCTURED pass/fail summary. Never fabricates\n# results: if the toolchain is missing it reports the detected command and stops.\n#\n# Idempotent and side-effect-free apart from the test run itself (which the project controls). Detect\n# mode is fully read-only.\n#\n# Usage:\n#   detect-and-run-tests.sh --detect [repo-root]              # identify framework + command only\n#   detect-and-run-tests.sh --run [--coverage] [repo-root]    # detect, then run\n#   detect-and-run-tests.sh --run --target "<expr>" [repo]    # run a scoped subset (framework syntax)\n#\n# Options:\n#   --detect        Detect framework and run command; do not execute. (Read-only.)\n#   --run           Detect, then execute the test command.\n#   --coverage      Add the framework\'s coverage flag when running.\n#   --target EXPR   Pass a framework-specific filter to scope the run (e.g. a test path/name).\n#   -h | --help     Help.\n#\n# Output is Markdown with a machine-readable RESULT block at the end:\n#   RESULT: framework=<f> command=<c> ran=<yes|no> status=<pass|fail|unknown> exit=<n>\n\nset -u\n\nPROG=$(basename "$0")\nMODE=""\nCOVERAGE=0\nTARGET=""\nROOT="."\n\nusage() { sed -n \'2,27p\' "$0" | sed \'s/^# \\{0,1\\}//\'; exit "${1:-0}"; }\n\nwhile [ $# -gt 0 ]; do\n  case "$1" in\n    --detect) MODE="detect" ;;\n    --run) MODE="run" ;;\n    --coverage) COVERAGE=1 ;;\n    --target) shift; TARGET="${1:-}" ;;\n    -h|--help) usage 0 ;;\n    -*) echo "ERROR: unknown option \'$1\'" >&2; usage 2 ;;\n    *) ROOT="$1" ;;\n  esac\n  shift\ndone\n\n[ -z "$MODE" ] && { echo "ERROR: pass --detect or --run." >&2; usage 2; }\n[ -d "$ROOT" ] || { echo "ERROR: repo-root \'$ROOT\' is not a directory." >&2; exit 1; }\n\nhas() { command -v "$1" >/dev/null 2>&1; }\nexists() { [ -e "$ROOT/$1" ]; }\nglob_exists() { set -- "$ROOT"/$1; [ -e "$1" ]; }\n\nFRAMEWORK="unknown"\nRUNNER=""           # binary that must exist to run\nCMD=""              # base command\nCOV_CMD=""          # command with coverage\nSCOPE_FLAG=""       # how --target is appended (printf format, %s = target)\n\n# --- Detection (strongest signal wins; checked in priority order) ---\ndetect() {\n  # Node / JS / TS\n  if exists "package.json"; then\n    FRAMEWORK="node"\n    RUNNER="node"\n    if grep -q \'"vitest"\' "$ROOT/package.json" 2>/dev/null || glob_exists "vitest.config.*"; then\n      FRAMEWORK="vitest"; CMD="npx vitest run"; COV_CMD="npx vitest run --coverage"; SCOPE_FLAG="%s"\n    elif grep -q \'"jest"\' "$ROOT/package.json" 2>/dev/null || glob_exists "jest.config.*"; then\n      FRAMEWORK="jest"; CMD="npx jest"; COV_CMD="npx jest --coverage"; SCOPE_FLAG="%s"\n    elif glob_exists ".mocharc.*" || grep -q \'"mocha"\' "$ROOT/package.json" 2>/dev/null; then\n      FRAMEWORK="mocha"; CMD="npx mocha"; COV_CMD="npx nyc mocha"; SCOPE_FLAG="%s"\n    elif grep -q \'"scripts"\' "$ROOT/package.json" 2>/dev/null && grep -qE \'"test" *:\' "$ROOT/package.json" 2>/dev/null; then\n      FRAMEWORK="npm-script"; CMD="npm test"; COV_CMD="npm test -- --coverage"; SCOPE_FLAG=""\n    else\n      FRAMEWORK="npm-script"; CMD="npm test"; COV_CMD="npm test"; SCOPE_FLAG=""\n    fi\n    RUNNER="npm"\n    return\n  fi\n  # Python\n  if exists "pyproject.toml" || exists "setup.cfg" || exists "pytest.ini" || exists "tox.ini" || glob_exists "requirements*.txt"; then\n    if exists "pytest.ini" || grep -lq "pytest" "$ROOT"/pyproject.toml "$ROOT"/setup.cfg "$ROOT"/tox.ini 2>/dev/null; then\n      FRAMEWORK="pytest"; RUNNER="pytest"; CMD="pytest"; COV_CMD="pytest --cov --cov-report=term-missing"; SCOPE_FLAG="%s"\n    else\n      FRAMEWORK="pytest"; RUNNER="pytest"; CMD="python -m pytest"; COV_CMD="python -m pytest --cov --cov-report=term-missing"; SCOPE_FLAG="%s"\n    fi\n    has pytest || RUNNER="python"\n    return\n  fi\n  # Java — Maven\n  if exists "pom.xml"; then\n    FRAMEWORK="maven"\n    if exists "mvnw"; then RUNNER="./mvnw"; CMD="./mvnw -q test"; else RUNNER="mvn"; CMD="mvn -q test"; fi\n    COV_CMD="$CMD jacoco:report"; SCOPE_FLAG="-Dtest=%s"\n    return\n  fi\n  # Java/Kotlin — Gradle\n  if exists "build.gradle" || exists "build.gradle.kts"; then\n    FRAMEWORK="gradle"\n    if exists "gradlew"; then RUNNER="./gradlew"; CMD="./gradlew test"; else RUNNER="gradle"; CMD="gradle test"; fi\n    COV_CMD="$CMD jacocoTestReport"; SCOPE_FLAG="--tests %s"\n    return\n  fi\n  # Go\n  if exists "go.mod"; then\n    FRAMEWORK="go"; RUNNER="go"; CMD="go test ./..."\n    COV_CMD="go test -coverprofile=cover.out ./..."; SCOPE_FLAG="-run %s"\n    return\n  fi\n  # Rust\n  if exists "Cargo.toml"; then\n    FRAMEWORK="cargo"; RUNNER="cargo"; CMD="cargo test"; COV_CMD="cargo test"; SCOPE_FLAG="%s"\n    has cargo-llvm-cov && COV_CMD="cargo llvm-cov"\n    return\n  fi\n  # .NET\n  if glob_exists "*.sln" || glob_exists "*.csproj"; then\n    FRAMEWORK="dotnet"; RUNNER="dotnet"; CMD="dotnet test"\n    COV_CMD="dotnet test --collect:\\"XPlat Code Coverage\\""; SCOPE_FLAG="--filter %s"\n    return\n  fi\n  # Ruby\n  if exists "Gemfile" && { exists ".rspec" || ls "$ROOT"/spec >/dev/null 2>&1; }; then\n    FRAMEWORK="rspec"; RUNNER="bundle"; CMD="bundle exec rspec"; COV_CMD="bundle exec rspec"; SCOPE_FLAG="%s"\n    return\n  fi\n  # PHP\n  if exists "composer.json" || exists "phpunit.xml"; then\n    FRAMEWORK="phpunit"; RUNNER="php"; CMD="vendor/bin/phpunit"; COV_CMD="vendor/bin/phpunit --coverage-text"; SCOPE_FLAG="--filter %s"\n    return\n  fi\n}\n\ndetect\n\necho "# Test framework detection: \\`$ROOT\\`"\necho\nif [ "$FRAMEWORK" = "unknown" ]; then\n  echo "**Framework: unknown.** No recognized manifest (package.json, pyproject.toml, pom.xml,"\n  echo "build.gradle, go.mod, Cargo.toml, *.csproj, Gemfile, composer.json) was found at the root."\n  echo\n  echo "RESULT: framework=unknown command=- ran=no status=unknown exit=2"\n  exit 2\nfi\n\n# Choose command (coverage variant if requested).\nRUN_CMD="$CMD"\n[ "$COVERAGE" -eq 1 ] && [ -n "$COV_CMD" ] && RUN_CMD="$COV_CMD"\n# Append scope filter if a target was given and the framework supports it.\nif [ -n "$TARGET" ] && [ -n "$SCOPE_FLAG" ]; then\n  # shellcheck disable=SC2059\n  RUN_CMD="$RUN_CMD $(printf "$SCOPE_FLAG" "$TARGET")"\nfi\n\necho "- **Framework:** $FRAMEWORK"\necho "- **Run command:** \\`$RUN_CMD\\`"\necho "- **Runner binary required:** \\`$RUNNER\\`"\n[ -n "$TARGET" ] && echo "- **Scoped to target:** \\`$TARGET\\`"\necho\n\n# Confirm the runner is available.\nRUNNER_BIN=$(printf \'%s\' "$RUNNER" | sed \'s#^\\./##\')\nif [ "${RUNNER#./}" != "$RUNNER" ]; then\n  # wrapper script in repo\n  AVAIL=$( [ -x "$ROOT/$RUNNER_BIN" ] && echo yes || echo no )\nelse\n  AVAIL=$( has "$RUNNER_BIN" && echo yes || echo no )\nfi\necho "- **Toolchain available:** $AVAIL"\necho\n\nif [ "$MODE" = "detect" ]; then\n  echo "RESULT: framework=$FRAMEWORK command=\\"$RUN_CMD\\" ran=no status=unknown exit=0"\n  exit 0\nfi\n\n# --- Run mode ---\nif [ "$AVAIL" = "no" ]; then\n  echo "> Cannot run: the required toolchain (\\`$RUNNER_BIN\\`) is not installed in this environment."\n  echo "> Reporting the command only — results were NOT executed and must not be claimed as passing."\n  echo\n  echo "RESULT: framework=$FRAMEWORK command=\\"$RUN_CMD\\" ran=no status=unknown exit=3"\n  exit 3\nfi\n\necho "## Test run output"\necho\necho \'```\'\n# Run from the repo root, capture combined output and exit code, stream a tail to the report.\nOUT=$( cd "$ROOT" && sh -c "$RUN_CMD" 2>&1 )\nCODE=$?\nprintf \'%s\\n\' "$OUT" | tail -n 120\necho \'```\'\necho\n\n# --- Parse a pass/fail summary heuristically across frameworks ---\nSUMMARY=$(printf \'%s\\n\' "$OUT" | grep -iE \\\n  \'passed|failed|fail|ok|error|tests? run|[0-9]+ passing|[0-9]+ failing|PASS|FAIL|test result:\' \\\n  | tail -n 8)\n\nSTATUS="unknown"\nif [ "$CODE" -eq 0 ]; then STATUS="pass"; else STATUS="fail"; fi\n\necho "## Summary"\necho\n[ -n "$SUMMARY" ] && { echo \'```\'; printf \'%s\\n\' "$SUMMARY"; echo \'```\'; }\necho "- Exit code: $CODE → **$STATUS**"\nif [ "$CODE" -ne 0 ]; then\n  echo "- Note: a non-zero exit can mean a real test failure OR an environment problem (missing"\n  echo "  service, dependency). Inspect the output above before concluding which."\nfi\necho\necho "RESULT: framework=$FRAMEWORK command=\\"$RUN_CMD\\" ran=yes status=$STATUS exit=$CODE"\nexit "$CODE"\n',
            bytes: 8296,
        },
        {
            path: 'assets/test-report-template.md',
            role: 'asset',
            content:
                '# Test Report\n\n> Fill every section from the ACTUAL run. Never report pass/fail counts you did not obtain from\n> executing the suite. If the suite could not run, say so plainly in "Run results".\n\n## Target\n\n<!-- The code under test: file(s)/module/function and what behavior it implements. -->\n\n## Framework and command\n\n- **Framework detected:** <pytest | jest | vitest | maven | gradle | go test | cargo | dotnet | ...>\n- **Command used:** `<exact command, e.g. ./gradlew test --tests "pkg.Class.method">`\n- **Scope:** <whole suite | scoped to target>\n\n## Test files added or changed\n\n- `path/to/test_x.py` — <new | extended>\n- ...\n\n## Scenarios covered\n\n| # | Scenario | Type (happy/boundary/error) | Asserts |\n|---|---|---|---|\n| 1 |  |  |  |\n\n<!-- One row per test. "Asserts" = the specific outcome verified, not "runs without error". -->\n\n## Run results (from the actual execution)\n\n- **Total:** <N>  |  **Passed:** <N>  |  **Failed:** <N>  |  **Skipped:** <N>\n- **Exit status:** <pass | fail>\n- **Coverage (if measured):** <branch/line %, and which uncovered branches remain>\n\n```\n<paste the relevant pass/fail summary lines from the runner output>\n```\n\n## Product bugs found\n\n<!-- If a test revealed a real defect in production code, describe it here. Do NOT weaken the test to\nhide it. If none, write "None — all assertions reflect intended behavior." -->\n\n## Harness fixes (if any)\n\n<!-- Any production-code change made solely to unblock the test harness, with justification. If none,\nwrite "None — production code unchanged." -->\n\n## Coverage gaps and recommended next tests\n\n- <high-risk branch/path still untested and why it matters>\n- ...\n\n## Environment notes\n\n<!-- Flaky/missing services, skipped suites, anything that affects how to read the results. -->\n\nTESTS_COMPLETE\n',
            bytes: 1794,
        },
    ],
    scripts: [{ name: 'detect-and-run-tests.sh', purpose: 'Detect test framework and run the test suite' }],
    install: { defaultLocation: '.claude/skills/test-runner/', supportsProject: true, supportsGlobal: true },
};
