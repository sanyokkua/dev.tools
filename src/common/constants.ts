/**
 * The default identifier for the plain text language mode in editor components.
 *
 * This constant is used as a fallback or initial value when no specific language mode has been selected.
 */
export const DEFAULT_LANGUAGE_ID: string = 'plaintext';
/**
 * Default file extension used when none is specified.
 * This value applies globally across the application's file operations and ensures consistent default behavior for new or unspecified files.
 */
export const DEFAULT_EXTENSION: string = '.txt';
/**
 * Default MIME type used when the content type is not explicitly specified.
 *
 * Represents the default textual data format if no specific MIME type can be determined.
 */
export const DEFAULT_MIME_TYPE: string = 'text/plain';
